import 'package:flutter/material.dart';

class CustomeIcon extends StatelessWidget {

  Icon icon;
  CustomeIcon(this.icon);

  @override
  Widget build(BuildContext context) {
    return icon;
  }
}

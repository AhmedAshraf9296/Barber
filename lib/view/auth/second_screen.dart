import 'package:flutter/material.dart';

class Second_Screen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AppBar(title: Text('HomeScreen'),),
    );
  }
}
